generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model portfolio {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  description String
  image       String
  name        String
  tags        String[]
  url         String
}
